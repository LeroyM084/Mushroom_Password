const textFieldValue = document.querySelector('input[type="email"]').value;
const submitButton = document.getElementById("submitButton");

submitButton.addEventListener(on)

function saveMailAPI(){

}